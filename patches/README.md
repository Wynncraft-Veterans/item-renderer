# patches/

The entire functional delta of this overlay. Each `*.patch` is applied in
sorted order against a fresh clone of `wynn-tools/wynn.tools` at the
`WYNN_TOOLS_REF` pinned in [`../docker/Dockerfile`](../docker/Dockerfile).

See the repo [README](../README.md) for the patch-regeneration workflow.
